{
	"name": "videosegments-api",
	"version": "1.0.0",
	"description": "backend for videosegments",
	"main": "app.js",
	"scripts": {
		"start": "SET NODE_ENV=dev && node app.js",
		"serve": "SET NODE_ENV=prod && node app.js",
		"test": "SET NODE_ENV=test && mocha --timeout 10000 --exit"
	},
	"author": "Alex Lys",
	"license": " GPL-3.0-or-later",
	"dependencies": {
		"bcryptjs": "^2.4.3",
		"cookie-parser": "^1.4.4",
		"cors": "^2.8.5",
		"express": "^4.16.4",
		"express-mysql-session": "^2.1.0",
		"express-session": "^1.15.6",
		"mysql": "^2.16.0",
		"passport": "^0.4.0",
		"passport-local": "^1.0.0",
		"request": "^2.88.0"
	},
	"devDependencies": {
		"chai": "^3.5.0",
		"chai-http": "^4.2.1",
		"mocha": "^6.0.2"
	}
}
